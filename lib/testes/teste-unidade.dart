import 'package:arquitetur_mvvm/dal/aluno-dao.dart';
import 'package:arquitetur_mvvm/paginas/formulario-aluno/formulario-aluno-controller.dart';
import 'package:arquitetur_mvvm/paginas/formulario-aluno/formulario-aluno-view.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Incluir novo aluno', () {    

    // var controller = FormularioAlunoController(FormularioAlunoState());    
    // var itens = AlunoDAO.instancia.liste();

    // controller.viewModel.id = 3;
    // controller.viewModel.nome = "João";
    // controller.salve();
    
    // var total = itens.length + 1;

    // expect(total, AlunoDAO.instancia.liste().length);

  });
}
